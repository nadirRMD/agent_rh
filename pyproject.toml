[project]
name = "agent-rh"
version = "0.1.0"
description = "Agent RH de base en structure aplatie"
readme = "README.md"
requires-python = ">=3.14"
authors = [
    { name = "mboumaiz" },
]
dependencies = [
    "deepagents>=0.6.2",
    "langchain>=1.3.1",
]

[project.scripts]
agent-rh = "main:main"
